on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      # To use this repository's private action, you must check out the repository
      - name: Checkout
        uses: actions/checkout@v1
      - name: Hello world action step
        uses: kyamaguchi/hello-world-javascript-action@9254891
        id: hello
        with:
          who-to-greet: 'Private Mona the Octocat with ncc'
      # Use the output from the `hello` step
      - name: Get the output time
        run: echo "The time was ${{ steps.hello.outputs.time }}"
      - name: Hello world docker action step
        id: hello-docker
        uses: kyamaguchi/hello-world-docker-action@8c2f67a
        with:
          who-to-greet: 'Mona the Docker'
      # Use the output from the `hello` step
      - name: Get the output time
        run: echo "The time was ${{ steps.hello-docker.outputs.time }}"
